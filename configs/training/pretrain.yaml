# @package _group_
output_dir: models/checkpoints/pretrain
num_epochs: 3
batch_size: 8
learning_rate: 5.0e-4
weight_decay: 0.01
max_grad_norm: 1.0
warmup_ratio: 0.1
eval_every: 500
save_every: 1000
logging_every: 10
device: auto
dtype: float32
compile_model: false